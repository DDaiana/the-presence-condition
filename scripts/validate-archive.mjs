import { access, readFile } from "node:fs/promises"; const records=JSON.parse(await readFile("data/photo-inventory.json","utf8")); let missing=0; for(const r of records.filter(x=>!x.exact_duplicate_of)){try{await access(`public/archive/${r.archive_id}.jpg`)}catch{missing++}} if(missing) throw new Error(`${missing} derivatives missing`); console.log(`Validated ${records.length} inventory records and all publishable derivatives.`);
