# Archive system

The source archive, interpretive Conditions and public publication are independent layers. Archive IDs are stable `TPC-00001` identifiers mapped to content hashes. Automated scans preserve manual overrides, keep unknown fields `null`, and default Condition decisions to manual review. Source paths and private metadata are internal only; public pages use generated derivatives and stable IDs.
