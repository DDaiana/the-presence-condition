import type {MetadataRoute} from "next"; import archive from "../data/public-archive.json"; export const dynamic="force-static"; export default function sitemap():MetadataRoute.Sitemap{const base="https://ddaiana.github.io/the-presence-condition";return ["","/archive","/condition","/about","/info","/copyright",...archive.map(p=>`/archive/${p.archive_id}`)].map(url=>({url:base+url}))}
