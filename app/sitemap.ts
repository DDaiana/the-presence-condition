import type {MetadataRoute} from "next"; import archive from "../data/public-archive.json"; export const dynamic="force-static"; export default function sitemap():MetadataRoute.Sitemap{const base="https://ddaiana.github.io/the-presence-condition";const visible=archive.filter(p=>p.curation_status==="CURATED"||p.curation_status==="SEQUENCE_MEMBER"||p.home_featured);return ["","/archive","/condition","/about","/info","/copyright",...visible.map(p=>`/archive/${p.archive_id}`)].map(url=>({url:base+url}))}
