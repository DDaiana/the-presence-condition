import metadata from "../../data/public-metadata.json"; import publicArchive from "../../data/public-archive.json"; import {Shell} from "../components"; import ArchiveGrid from "./archive-grid";
export default function Archive(){const source=new Map(metadata.map(p=>[p.archive_id,p]));const entries=publicArchive.map(p=>({...p,public_date:source.get(p.archive_id)?.public_date??null}));return <Shell><div className="title-row"><div><h1>THE PRESENCE ARCHIVE</h1></div><span>VIEW ⠿</span></div><ArchiveGrid entries={entries}/></Shell>}
